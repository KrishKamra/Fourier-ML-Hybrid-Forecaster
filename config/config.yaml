# Fourier-Enhanced AI Configuration

data:
  window_size: 48
  forecast_horizon: 1
  n_points_synthetic: 1000

fourier:
  top_k_frequencies: 3
  detrend: true

model:
  type: "xgboost"
  n_estimators: 100
  learning_rate: 0.1
  max_depth: 5

api:
  host: "127.0.0.1"
  port: 8000